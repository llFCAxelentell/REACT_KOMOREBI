import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FF9900',
    },
    secondary: {
      main: '#EEEEEE',
    },
    text:{
      main:"#FFFFFF"
    }
  },
});

export default theme;